"""Phase 1: Domain layer (business logic, entities, exceptions)

This layer contains:
- Entities: Core domain objects (Lead, EmailSequence, Event, User)
- Value Objects: Immutable objects (Email, URL, TrackingId)
- Exceptions: Domain-specific exceptions
- Repositories: Abstract interfaces for data access

The domain layer is independent of frameworks and infrastructure.
It represents the business rules and constraints of the application.
"""
