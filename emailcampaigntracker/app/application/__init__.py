"""Phase 1: Application layer (use cases, application services)

This layer contains:
- Application Services: Orchestrate domain logic, coordinate repositories
- DTOs: Data Transfer Objects for API requests and responses
- Use Cases: High-level business workflows

The application layer depends on the domain layer but is independent
of infrastructure (database, external services, HTTP, etc.).
"""
